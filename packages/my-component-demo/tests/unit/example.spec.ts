import { shallowMount } from '@vue/test-utils';
import App from '@/App.vue';

describe('App.vue', () => {
  // it('should be a text in a component', () => {
  //   const coderName = 'Andrea';
  //   const wrapper = shallowMount(App, {
  //     propsData: { coderName },
  //   });
  //   expect(wrapper.text()).toMatch(coderName);
  // });
});
